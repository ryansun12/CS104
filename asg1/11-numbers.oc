// $Id: 11-numbers.oc,v 1.8 2019-04-09 16:41:36-07 - - $

#include "oclib.h"

int main() {
   int number = 1;
   while (number > 0) {
      putint (number);
      putchr ('\n');
      number = number + number;
   }
   putint (number);
   putchr ('\n');
   return EXIT_SUCCESS;
}

